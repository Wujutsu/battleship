{% extends 'base.html.twig' %}

{% block body %}
<div class="row">
    <h2 class="text-center mt-5">Bataille Navale Online</h2>
    <input type="hidden" id="token" value="{{ randomString }}">
    <div class="linkUrlPlay mt-4">https://127.0.0.1:8000/game/guest/{{ randomString }}</div>
    <div class="col-12 mt-5">
        <button class="btn btn-primary btnPlay">JOUER</button>
    </div>
</div>
{% endblock %}